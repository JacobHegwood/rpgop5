const state = {
  status: "",
  token: localStorage.getItem("token") || "",
  user: {}
};

const actions = {};

const mutations = {};

const getters = {};

export default { state, actions, mutations, getters };
